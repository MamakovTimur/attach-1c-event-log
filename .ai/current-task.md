# Устранение дефектов полного аудита — 1.3.38

**Статус:** готово 07.09.2026. EPF пересобран на 8.3.27.2325, `smoke_open_epf.py` PASS.

## Сделано
- Устранён серверный вызов клиентской процедуры; межконтекстный аудит включён в `run_all.py`.
- Backup manifest V2, защита незавершённых копий и восстановление точного набора файлов.
- Staging-публикация файлов, потоковый merge/перекодировка UTF-8 и UTF-16.
- Дедупликация учитывает все поля и кратность записей; split-targets входят в backup.
- Python CLI атомарно публикует файлы, валидирует `.lgp`, действия и одинаковые каталоги.

## Проверки
- `tests/run_all.py`: 25 unittest + 36 module-level, golden неизменны.
- `syntaxcheck`: Module.bsl и ObjectModule.bsl без syntax/error/critical.
- `1c-code-reviewer`: блокирующих замечаний нет.

## Результат
Открывать свежий `ПрисоединениеЖурналаРегистрации.epf` версии 1.3.38. Экспериментальные dedup/split по-прежнему использовать только на копии журнала.
